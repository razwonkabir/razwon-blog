{
  "entities": {
    "BlogPost": {
      "title": "BlogPost",
      "description": "A single publication or article inside the Razwon developer blog.",
      "type": "object",
      "properties": {
        "title": {
          "type": "string",
          "description": "The publication title."
        },
        "summary": {
          "type": "string",
          "description": "A brief summary of the article's contents."
        },
        "content": {
          "type": "string",
          "description": "Complete markdown-formatted content body."
        },
        "tags": {
          "type": "array",
          "description": "List of tags associated with this post."
        },
        "categories": {
          "type": "array",
          "description": "List of categories associated with this post."
        },
        "author": {
          "type": "string",
          "description": "Name of the post author."
        },
        "createdAt": {
          "type": "number",
          "description": "Timestamp when the post was published."
        },
        "readTime": {
          "type": "string",
          "description": "Estimated duration to read the article."
        },
        "coverImage": {
          "type": "string",
          "description": "Optional URL to a cover image."
        },
        "status": {
          "type": "string",
          "description": "Draft or published status of the post.",
          "enum": ["draft", "published"]
        },
        "views": {
          "type": "number",
          "description": "View counter."
        }
      },
      "required": ["title", "summary", "content", "tags", "categories", "author", "createdAt", "readTime"]
    },
    "Subscription": {
      "title": "Subscription",
      "description": "Newsletter subscription registration.",
      "type": "object",
      "properties": {
        "email": {
          "type": "string",
          "description": "The subscriber's email address."
        },
        "subscribedAt": {
          "type": "number",
          "description": "Timestamp of subscription."
        },
        "source": {
          "type": "string",
          "description": "Source campaign or page of subscription."
        }
      },
      "required": ["email", "subscribedAt", "source"]
    }
  },
  "firestore": {
    "/posts/{postId}": {
      "schema": "BlogPost",
      "description": "Global collection containing all blog publications."
    },
    "/subscriptions/{subId}": {
      "schema": "Subscription",
      "description": "Collection containing all email newsletter subscriptions."
    }
  }
}
